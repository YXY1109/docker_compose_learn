apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sanic-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sanic-app
                port:
                  number: 80
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sanic-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sanic-app
  template:
    metadata:
      labels:
        app: sanic-app
    spec:
      containers:
      - name: sanic-app
        image: ghcr.io/<你的GitHub用户名>/my-sanic-app:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"